/*
 * These are DisplayType objects (NOT strings).
 * These are the ONLY 5 DisplayType objects possible - a fixed set of named instances.
 * Referenced as DisplayType.LED, DisplayType.LCD, etc.
 */
enum DisplayType {
    LED, OLED, PLASMA, LCD, CRT
}